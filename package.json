{
  "name": "app",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:seki221/graduation-exam.git",
  "license": "MIT",
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/stimulus,": "hotwired/turbo-rails",
    "@rails/activestorage": "^8.0.0",
    "@rails/ujs": "^7.1.3-4",
    "esbuild": "^0.24.0"
  },
  "scripts": {
    "build": "esbuild app/javascript/**/* --bundle --sourcemap --outdir=app/assets/builds --public-path=assets",
    "watch": "esbuild app/javascript/**/* --bundle --sourcemap --outdir=app/assets/builds --watch"
  },
  "devDependencies": {
    "eslint": "^9.15.0"
  }
}


<div class="wrapper">
  <%= render 'shared/planTabs' %>
  <div class="sectionMain d-flex">
    <%# 編集ボタン %>
    
    <div class="plan">
      <%= image_tag '/assets/default.jpg', class: "card-img-top" %>
      <ul class="informations">
        <li class="information">
          <p class="title">しおりタイトル</p>
          <p class="content"><%= @planner.title.presence || '未登録' %></p>
        </li>
        <li class="information">
          <p class="title">旅行期間</p>
          <p class="content">
            <span class="start"><%= l(@planner.start_date, format: :default) %></span> ~ 
            <span class="end"><%= l(@planner.end_date, format: :default) %></span>
          </p>
        </li>
      </ul>
    </div>
      <div class="d-flex-edit" style="flex-direction: column">
        <% if @planner.user == current_user %>
          <%= link_to edit_planner_path(@planner), class: 'buttonedit' do %>
            <p class="buttontitle">しおり情報編集</p>
          <% end %>
        <%= link_to "しおりを削除", planner_path(@planner), data: { turbo_method: "delete", turbo_confirm: "削除しますか？" }, class: "buttonedit" %>
        <% end %>
      </div>
    </div>
  </div>
</div>

<div class="wrapper">
  <%= render 'shared/planTabs' %>
  <div class="sectionMain">
    <%# 編集ボタン %>
    <div class="button-group">
      <%= button_tag(type: 'button', class: 'buttonedit') do %>
        <p class="buttontitle">スケジュール情報編集</p>
      <% end %>
    </div>
    <div class="plan">
      <div class="lists">
        <div class="header">
          <ul class="informations">
            <li class="information">
              <p class="content">
                <span class="start"><%= l(@planner.start_date, format: :default) %></span> ~ 
                <span class="end"><%= l(@planner.end_date, format: :default) %></span>
              </p>
            </li>
            <li class="information">
              <p class="title">しおりタイトル</p>
              <p class="content"><%= @schedule.title.presence || '未登録' %></p>
            </li>
            <div class="list">
              <p class="title">場所</p>
              <p class="content"><%= @schedule.place.presence || '未登録'  %></p>
            </div>
            <div class="list">
              <p class="title">費用</p>
              <p class="content"><%= @schedule.cost.presence || '未登録' %></p>
            </div>
            <div class="list">
              <p class="title">説明</p>
              <p class="content"><%= @schedule.guaid.presence || '未登録' %></p>
            </div>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>
